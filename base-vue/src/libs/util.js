import * as cookies from './util.cookies';
import * as menu from './util.menu';
import * as tool from './util.tool';
import * as regular from './util.regular';
import * as error from './util.error';
import * as win from './util.win';

const util = {
    cookies,
    menu,
    tool,
    regular,
    error,
    win,
};

export default util;
